name: Deploy to Development

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Node.js (or Python, etc.)
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Run Tests
        run: npm test

      - name: Deploy to Development Server
        run: echo "Deploying to development server..."
        # Replace this with actual deployment commands (e.g., SSH, FTP, or AWS/GCP CLI)
